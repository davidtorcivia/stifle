<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext only for local development (10.0.2.2 is Android emulator localhost) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    
    <!-- Production: require HTTPS with certificate pinning -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">stifle.app</domain>
        <domain includeSubdomains="true">api.stifle.app</domain>
        <!-- Add certificate pins when you have production certs -->
        <!-- <pin-set>
            <pin digest="SHA-256">base64_encoded_pin_here</pin>
        </pin-set> -->
    </domain-config>
    
    <!-- Default: no cleartext -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
